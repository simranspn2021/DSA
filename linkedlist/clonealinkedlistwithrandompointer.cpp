//we can clone a linkedlist and a random pointers using 1 appraoch in which we clone a whole linked list 
//and then using loops can fnd the distance btw random pointers and can get to the proper random pointers 
//but this approach uses O(n2) time which is high so 
//2 approach is:
//watch the video completely 
